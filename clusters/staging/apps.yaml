apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: apps
  namespace: flux-system
spec:
  targetNamespace: default
  interval: 1m
  path: ../base
  prune: true
  sourceRef:
    kind: GitRepository
    name: repo
    namespace: flux-system
